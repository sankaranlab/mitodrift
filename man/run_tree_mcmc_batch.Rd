% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mitodrift.R
\name{run_tree_mcmc_batch}
\alias{run_tree_mcmc_batch}
\title{Run tree-topology MCMC in batches with convergence monitoring}
\usage{
run_tree_mcmc_batch(
  phy_init,
  logP_list,
  logA_vec,
  outfile,
  diagfile = NULL,
  diag = TRUE,
  max_iter = 10000,
  nchains = 1,
  ncores = 1,
  ncores_qs = 1,
  batch_size = 1000,
  conv_thres = NULL,
  resume = FALSE,
  mc3_temperatures = NULL,
  mc3_swap_interval = 10L,
  mc3_ncores = NULL,
  mc3_statefile = NULL,
  checkpoint_every = 1L,
  return_trace = TRUE
)
}
\arguments{
\item{phy_init}{A rooted \code{phylo} object used as the starting tree.}

\item{logP_list}{List of log-probability vectors (one per locus).}

\item{logA_vec}{Numeric vector of log transition probabilities.}

\item{outfile}{File path identifying the trace (qs2 format). Checkpoints
are written as small per-checkpoint block files under \verb{<outfile>.blocks/}
rather than rewriting one cumulative file; a legacy single file directly
at \code{outfile} (as archived regression keys use) is also still read
transparently. Use \code{safe_read_chain()} rather than \code{qs2::qd_read()}
directly to read a trace written by this function.}

\item{diagfile}{Optional file path for saving convergence diagnostics
(RDS format).}

\item{diag}{Logical; whether to compute diagnostics (currently unused,
diagnostics are always computed).}

\item{max_iter}{Integer; total number of MCMC iterations per chain
(ignored when \code{conv_thres} is set).}

\item{nchains}{Integer; number of independent chains.}

\item{ncores}{Integer; number of threads for C++ MCMC sampling.}

\item{ncores_qs}{Integer; number of threads for qs2 serialization.}

\item{batch_size}{Integer; number of iterations per batch.}

\item{conv_thres}{Numeric or \code{NULL}; if set, run until the ASDSF drops
below this threshold instead of using \code{max_iter}.}

\item{resume}{Logical; if \code{TRUE}, resume from existing \code{outfile}.}

\item{mc3_temperatures}{Optional numeric temperature ladder for
Metropolis-coupled MCMC. It must start at 1 and strictly increase. \code{NULL}
or the single value \code{1} runs the original independent-chain sampler.}

\item{mc3_swap_interval}{Positive integer; iterations between proposed
swaps of randomly selected adjacent temperatures.}

\item{mc3_ncores}{Optional number of sampling threads for MC3. Defaults to
\code{ncores}; set it up to \code{nchains * length(mc3_temperatures)} to update all
ensemble-temperature pairs concurrently when those CPUs are allocated.}

\item{mc3_statefile}{Optional RDS checkpoint for heated-chain states and
swap statistics. Defaults to \code{paste0(outfile, ".mc3_state.rds")}.}

\item{checkpoint_every}{Positive integer; persist a new checkpoint and the
sampler state every this many diagnostic batches. The final batch is always
persisted. Values greater than one reduce I/O at the cost of re-running up
to \code{checkpoint_every - 1} batches after interruption. Each checkpoint is
written as its own small block file (see \code{outfile}), so this cost no
longer grows with total run length.}

\item{return_trace}{Logical; if \code{FALSE}, skip reconstructing the full
trace for the return value and return \code{NULL} instead. The checkpointed
file(s) on disk are unaffected either way. Set \code{FALSE} when the caller
only needs the on-disk trace (e.g. to avoid paying for a read-back that
would just be discarded).}
}
\value{
A list of edge-list chains (one list of edge matrices per chain),
or \code{NULL} when \code{return_trace = FALSE}.
}
\description{
Runs MCMC sampling in fixed-size batches, computing ASDSF convergence
diagnostics between batches. Supports automatic stopping when ASDSF
drops below a threshold and resume from a previous run.
}
\keyword{internal}
